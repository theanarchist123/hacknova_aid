import 'package:flutter/material.dart';

void main() {
  // Test basic Flutter types
  const offset = Offset(10.0, 20.0);
  const color = Colors.blue;
  
  print('Offset: $offset');
  print('Color: $color');
  print('Flutter types are working correctly!');
}
